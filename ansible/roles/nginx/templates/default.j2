server {
  listen 80;

  root {{ document_root }};
  index index.html index.htm;

  server_name 127.0.0.1;

  location / { 
    default_type "text/html";
    try_files $uri.html $uri $uri/ =404;
  }
}